<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content" android:layout_height="wrap_content"
    android:layout_margin="3dp"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Fried Rice with Chicken"
        android:textColor="@android:color/black"
        android:paddingTop="5dp"
        android:layout_centerInParent="true"
        android:textAlignment="center"
        android:layout_alignLeft="@id/popularImageView"
        android:layout_alignRight="@id/popularImageView"
        android:layout_below="@id/popularImageView"
        android:id="@+id/popularTextView"/>
    <ImageView
        android:id="@+id/popularImageView"
        android:layout_width="100dp"
        android:layout_height="80dp"
        android:scaleType="fitXY"
        tools:src="@drawable/img2" />

</RelativeLayout>